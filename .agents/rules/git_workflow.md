# Automatic Git Pushing

When you make changes to a user's codebase, you must always automatically commit and push those updates to the configured remote repository (GitHub) as your final step, unless the user explicitly asks you not to.

- Use clear and descriptive commit messages summarizing the task you just completed.
- Ensure all relevant files are tracked and added before committing.
